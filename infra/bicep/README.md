# Infrastructure (Bicep)

Planned in **milestone 5** (see [ROADMAP.md](../../ROADMAP.md)).

Bicep templates for the Azure footprint: AKS (small node pool), Azure SQL (Basic), Azure Cache for Redis (Basic), ACR. Includes an up/down deployment script so the environment can be torn down between demos to control cost.
