apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-api
spec:
  replicas: 1
  selector:
    matchLabels: { app: order-api }
  template:
    metadata:
      labels: { app: order-api }
    spec:
      containers:
        - name: order-api
          image: ${ACR_LOGIN_SERVER}/order-api:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef: { name: app-config }
          env:
            - name: ConnectionStrings__DefaultConnection
              valueFrom: { secretKeyRef: { name: app-secrets, key: OrderDbConnectionString } }
            - name: Jwt__Key
              valueFrom: { secretKeyRef: { name: app-secrets, key: JwtKey } }
            - name: ServiceBus__ConnectionString
              valueFrom: { secretKeyRef: { name: app-secrets, key: ServiceBusConnectionString } }
          resources:
            requests: { cpu: 50m, memory: 96Mi }
            limits: { cpu: 250m, memory: 256Mi }
          readinessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 30
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: order-api
spec:
  selector: { app: order-api }
  ports:
    - port: 80
      targetPort: 8080
