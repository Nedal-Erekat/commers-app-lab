apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-api
spec:
  replicas: 1
  selector:
    matchLabels: { app: identity-api }
  template:
    metadata:
      labels: { app: identity-api }
    spec:
      containers:
        - name: identity-api
          image: ${ACR_LOGIN_SERVER}/identity-api:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef: { name: app-config }
          env:
            - name: ConnectionStrings__DefaultConnection
              valueFrom: { secretKeyRef: { name: app-secrets, key: IdentityDbConnectionString } }
            - name: Jwt__Key
              valueFrom: { secretKeyRef: { name: app-secrets, key: JwtKey } }
          resources:
            requests: { cpu: 50m, memory: 96Mi }
            limits: { cpu: 250m, memory: 256Mi }
          readinessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 30
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: identity-api
spec:
  selector: { app: identity-api }
  ports:
    - port: 80
      targetPort: 8080
